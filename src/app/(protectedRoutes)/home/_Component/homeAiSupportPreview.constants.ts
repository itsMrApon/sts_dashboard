/** Layout tokens for the AI Support preview on Home — shared with lazy wrapper to keep `page.tsx` minimal. */
export const HOME_AI_SUPPORT_FEATURE_CLASS = 'border-0 bg-background'

export const HOME_AI_CHAT_OUTER_CLASS =
  'mx-auto h-[490px] w-full max-w-[430px] overflow-hidden rounded-xl bg-background'

/** Conversions column only — does not affect embedded ChatClient or public chat. */
export const HOME_CONVERSIONS_SECTION_CLASS = '-translate-y-6'
